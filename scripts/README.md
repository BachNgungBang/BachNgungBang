# Scripts

This folder is reserved for repeatable local helpers such as link validation, SVG checks, or asset generation.

Every script added here must document:

- its input and output;
- how to run it;
- any required environment variables or secrets;
- whether it changes files.

Do not use scripts to overwrite `README.md` content automatically without a reviewed source of truth.
